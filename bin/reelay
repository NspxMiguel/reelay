#!/usr/bin/env python3
"""Entrada do Reelay — roda direto do clone, sem instalar nada."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from reelay.cli import main

raise SystemExit(main())
